#include <stdio.h>
#include <math.h>
int main()
{
    // for (initialisation; condition; increment/decrement)
    // {
    //     /* do something
    // }

    for (int i = 1; i <= 20; i += 3)
    {
        printf("%d \n", i);
    }
    return 0;
}